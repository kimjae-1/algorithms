import sys
from collections import deque
sys.stdin = open('./solved.ac/level3/맥주/input.txt', 'r')
input = sys.stdin.readline


t = int(input().rstrip()) # test case
for _ in range(t):
    n = int(input().rstrip()) # the number of convience stores for each test case
    case_matrix = 
